"""The examples package no longer needs a BUILD-defined plc toolchain.

The prebuilt plc module extension generates the repository and toolchain
itself, so this root package is intentionally empty.
"""
