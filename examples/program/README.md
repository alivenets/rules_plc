# program

A `PROGRAM` (the cyclically-executed PLC POU, with persistent state across
calls) as an `st_binary`'s `program`: `st_binary` generates the `FUNCTION
main` entry point that instantiates and calls it.
