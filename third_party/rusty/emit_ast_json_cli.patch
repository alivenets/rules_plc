--- a/compiler/plc_driver/src/cli.rs
+++ b/compiler/plc_driver/src/cli.rs
@@ -86,6 +86,14 @@
     pub print_ast: bool,
 
     #[clap(
+        long = "ast-json",
+        group = "format",
+        global = true,
+        help = "Emit AST (Abstract Syntax Tree) as JSON output"
+    )]
+    pub print_ast_json: bool,
+
+    #[clap(
         long = "ast-lowered",
         group = "format",
         global = true,
